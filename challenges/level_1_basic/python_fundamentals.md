# Level 1 — Python Fundamentals

## Objectives
- Write idiomatic Python
- Add simple unit tests
- Pass linters (black, flake8), type checks (mypy), and tests (pytest)

## Tasks
1. Create a function `add(a: int, b: int) -> int` in `src/basics/math_utils.py`.
2. Write tests in `challenges/level_1_basic/tests/test_math_utils.py`.
3. Ensure `black`, `flake8`, `mypy`, and `pytest` all pass locally.

## Acceptance Criteria
- All checks pass locally and in CI
- Clear docstrings and type hints
