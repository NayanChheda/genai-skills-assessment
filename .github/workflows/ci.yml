name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Activate virtual environment (Windows PowerShell)
      run: |
        if (Test-Path .venv/Scripts/activate) {
          .\.venv\Scripts\activate
          Write-Host "✅ Virtual environment activated"
        } else {
          Write-Host "ℹ️ Virtual environment not found, using system Python"
        }
        python --version
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    - name: Lint with flake8
      run: |
        flake8 src/ challenges/ --count --max-complexity=10 --statistics
    - name: Check formatting with Black
      run: |
        black --check src/ challenges/
    - name: Type checking with mypy
      run: |
        mypy src/
    - name: Test with pytest and coverage
      run: |
        pytest --cov=src --cov-report=xml --cov-report=term-missing
      continue-on-error: true
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      if: success() || failure() 
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
    - name: Run automated scoring
      run: |
        python automated_scoring.py